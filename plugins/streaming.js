const PLUGIN_NAME = 'LivetooterStreaming'
const PACKAGE_NAME = 'livetooter-streaming'
const PROPERTY_NAME = 'streaming'

const LivetooterStreaming = {
  types: TYPES,
  propertyName: PROPERTY_NAME,
  installed: false,
  install(Vue, options = {}) {
  },
}

export default LivetooterStreamingn